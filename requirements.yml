---
collections:
  # Proxmox VM provisioning (proxmox_kvm moved here from community.general in 2025)
  - name: community.proxmox
    version: ">=1.0.0"
  # Windows post-config (optional: AD/NTP tweaks once the DC is reachable over WinRM)
  - name: ansible.windows
    version: ">=2.0.0"
  - name: community.windows
    version: ">=2.0.0"
